version: "3"
services:
  web:
    build: .
    #deploy:
      #replicas: 5
      #resources:
        #limits:
          #cpus: "0.1"
          #memory: 50M
      #restart_policy:
        #condition: on-failure
    ports:
      - "80:80"
    networks:
      - webnet
    links:
      - mongo
  mongo:
    image: 'mongo:3.4.1'
    ports:
      - '27017:27017'
    volumes:
      - './mongo:/data/db'
networks:
  webnet:

